# Copy relevant values into .claude/secrets/.env via scripts/vault.sh — never edit
# this file with real values, and never commit real values anywhere in this repo.

# Primary Gemini API key (Google AI Studio). Used for Gemini CLI research/review tasks.
GEMINI_API_KEY=

# Optional additional Gemini keys, for rotation / spreading rate limits across projects.
GEMINI_API_KEY_2=
GEMINI_API_KEY_3=
